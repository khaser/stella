language core;

extend with #records;
extend with #sum-types;

fn test(n : Nat) -> {a : Nat, b : Bool} + Nat {
  return inl({ a = 0, b = false })
}

fn main(n : Nat) -> {a : Nat} + Nat {
  return test(n)
}
